{
  "compilerOptions": {
    "target": "es6", // 사용할 JavaScript 버전
    "module": "esnext", // ES 모듈 방식 사용
    "checkJs": false, // TypeScript가 JS 파일에서 오류를 검사하지 않도록 설정
    "jsx": "preserve", // Vue와 함께 JSX를 사용할 경우 설정
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "exclude": [
    "node_modules",
    "dist",
    "**/*.ts", // TypeScript 파일 제외
    "**/*.vue" // Vue 파일에서 TypeScript 검사 제외
  ]
}
